<h3 class="center-align">All Users</h3>

<!-- Loop over each object in the projects array -->
<div class="container">
<table>
    <thead>
        <tr>
            <th>Is Admin</th>
            <th>Admin Requested?</th>
            <th>Make Admin?</th>
            <th>Full Name</th>
            <th>email</th>
        </tr>
    </thead>
    {{#each users as |user| }}
    <tbody>
        <td>{{admin}}</td>
        <td>{{request_admin}}</td>
        {{#unless request_admin}}
        {{!-- <td><i class="small material-icons">insert_chart</i></td> --}}
        <td>N/A</td>
        {{else}}
        <td><label>
            <input type="checkbox" />
            <span>Grant Admin Priveledges?</span>
        </label></td>
        {{/unless}}
        <td>{{full_name}}</td>        
        <td>{{email}}</td>
    </tbody>
    {{!-- <p>
        Order ID: {{order.id}}
      {{ <a class="text-decoration-none" href="/post/{{post.id}}">{{post.title}}</a> --}}
{{/each}}
</table>
</div>